# OpenClaw Dashboard 动态配置管理方案

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     OpenClaw Dashboard                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  通讯渠道配置    │  │  AI 模型配置    │  │  系统设置       │  │
│  │  - Telegram     │  │  - OpenAI       │  │  - 默认模型     │  │
│  │  - Discord      │  │  - Claude       │  │  - 默认渠道     │  │
│  │  - Slack        │  │  - OpenRouter   │  │  - 日志级别     │  │
│  │  - WhatsApp     │  │  - Gemini       │  │                 │  │
│  │  - 飞书/Lark    │  │  - MiniMax      │  │                 │  │
│  │  - iMessage     │  │  - DeepSeek     │  │                 │  │
│  │  - 其他         │  │  - 其他         │  │                 │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                        tRPC API Layer                           │
├─────────────────────────────────────────────────────────────────┤
│                     MySQL Database (TiDB)                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │ channel_configs │  │  model_configs  │  │ system_settings │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                   OpenClaw Gateway (本地)                        │
│              自动同步配置到 openclaw.json                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、数据库设计

### 2.1 通讯渠道配置表 (channel_configs)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| userId | INT | 关联用户 |
| channelType | ENUM | 渠道类型 |
| name | VARCHAR(100) | 配置名称（如"我的TG机器人"）|
| enabled | BOOLEAN | 是否启用 |
| config | JSON | 渠道特定配置（加密存储）|
| lastTestedAt | TIMESTAMP | 最后测试时间 |
| testStatus | ENUM | 测试状态：pending/success/failed |
| testMessage | TEXT | 测试结果消息 |
| createdAt | TIMESTAMP | 创建时间 |
| updatedAt | TIMESTAMP | 更新时间 |

**channelType 枚举值：**
- `telegram` - Telegram Bot
- `discord` - Discord Bot
- `slack` - Slack App
- `whatsapp` - WhatsApp Business API
- `feishu` - 飞书/Lark
- `lark` - Lark (国际版)
- `imessage` - iMessage (需要 Mac 服务器)
- `wechat` - 微信 (企业微信)
- `custom` - 自定义 Webhook

**config JSON 结构示例：**

```json
// Telegram
{
  "botToken": "123456:ABC-DEF...",
  "webhookUrl": "https://...",
  "allowedUsers": ["user1", "user2"],
  "dmPolicy": "open" | "pair" | "restricted"
}

// Discord
{
  "botToken": "MTIz...",
  "applicationId": "...",
  "guildIds": ["..."],
  "permissions": "..."
}

// Slack
{
  "botToken": "xoxb-...",
  "appToken": "xapp-...",
  "signingSecret": "...",
  "channels": ["#general"]
}

// WhatsApp
{
  "phoneNumberId": "...",
  "accessToken": "...",
  "verifyToken": "...",
  "businessAccountId": "..."
}

// 飞书/Lark
{
  "appId": "cli_...",
  "appSecret": "...",
  "encryptKey": "...",
  "verificationToken": "..."
}

// iMessage
{
  "appleId": "...",
  "password": "...", // 或 App-specific password
  "serverUrl": "..." // BlueBubbles/AirMessage 服务器
}
```

### 2.2 AI 模型配置表 (model_configs)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| userId | INT | 关联用户 |
| provider | ENUM | 提供商类型 |
| name | VARCHAR(100) | 配置名称 |
| enabled | BOOLEAN | 是否启用 |
| isDefault | BOOLEAN | 是否为默认模型 |
| config | JSON | 提供商特定配置（加密存储）|
| models | JSON | 可用模型列表 |
| lastTestedAt | TIMESTAMP | 最后测试时间 |
| testStatus | ENUM | 测试状态 |
| testMessage | TEXT | 测试结果 |
| createdAt | TIMESTAMP | 创建时间 |
| updatedAt | TIMESTAMP | 更新时间 |

**provider 枚举值：**
- `openai` - OpenAI (GPT-4, GPT-4o, etc.)
- `anthropic` - Anthropic (Claude)
- `openrouter` - OpenRouter (多模型聚合)
- `google` - Google (Gemini)
- `minimax` - MiniMax
- `deepseek` - DeepSeek
- `moonshot` - Moonshot (Kimi)
- `zhipu` - 智谱 AI
- `baichuan` - 百川
- `qwen` - 通义千问
- `custom` - 自定义 OpenAI 兼容 API

**config JSON 结构示例：**

```json
// OpenAI
{
  "apiKey": "sk-...",
  "baseUrl": "https://api.openai.com/v1", // 可选，支持代理
  "organization": "org-..." // 可选
}

// Anthropic
{
  "apiKey": "sk-ant-...",
  "baseUrl": "https://api.anthropic.com" // 可选
}

// OpenRouter
{
  "apiKey": "sk-or-v1-...",
  "siteUrl": "https://...", // 可选，用于排行榜
  "siteName": "OpenClaw" // 可选
}

// Google Gemini
{
  "apiKey": "AIza...",
  "projectId": "..." // 可选，用于 Vertex AI
}

// MiniMax
{
  "apiKey": "...",
  "groupId": "..."
}

// 自定义 OpenAI 兼容
{
  "apiKey": "...",
  "baseUrl": "https://your-api.com/v1",
  "modelMapping": {
    "gpt-4": "your-model-name"
  }
}
```

**models JSON 结构：**

```json
[
  {
    "id": "gpt-4o",
    "name": "GPT-4o",
    "contextLength": 128000,
    "inputPrice": 2.5,  // per 1M tokens
    "outputPrice": 10,
    "capabilities": ["chat", "vision", "function_calling"]
  }
]
```

### 2.3 系统设置表 (system_settings)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| userId | INT | 关联用户 |
| key | VARCHAR(100) | 设置键 |
| value | JSON | 设置值 |
| updatedAt | TIMESTAMP | 更新时间 |

**常用设置键：**
- `default_model` - 默认 AI 模型
- `default_channel` - 默认通讯渠道
- `gateway_config` - Gateway 配置
- `log_level` - 日志级别
- `auto_sync` - 是否自动同步到 OpenClaw

---

## 三、API 设计

### 3.1 通讯渠道 API

```typescript
// 获取所有渠道配置
channels.list() → ChannelConfig[]

// 获取单个渠道配置
channels.get({ id }) → ChannelConfig

// 创建渠道配置
channels.create({
  channelType: string,
  name: string,
  config: object
}) → ChannelConfig

// 更新渠道配置
channels.update({
  id: number,
  name?: string,
  enabled?: boolean,
  config?: object
}) → ChannelConfig

// 删除渠道配置
channels.delete({ id }) → { success: boolean }

// 测试渠道连接
channels.test({ id }) → {
  success: boolean,
  message: string,
  details?: object
}

// 同步到 OpenClaw
channels.syncToGateway({ id }) → { success: boolean }
```

### 3.2 AI 模型 API

```typescript
// 获取所有模型配置
aiModels.list() → ModelConfig[]

// 获取单个模型配置
aiModels.get({ id }) → ModelConfig

// 创建模型配置
aiModels.create({
  provider: string,
  name: string,
  config: object
}) → ModelConfig

// 更新模型配置
aiModels.update({
  id: number,
  name?: string,
  enabled?: boolean,
  isDefault?: boolean,
  config?: object
}) → ModelConfig

// 删除模型配置
aiModels.delete({ id }) → { success: boolean }

// 测试模型连接
aiModels.test({ id }) → {
  success: boolean,
  message: string,
  latency?: number,
  models?: string[]
}

// 获取可用模型列表
aiModels.fetchAvailableModels({ id }) → {
  models: Array<{
    id: string,
    name: string,
    contextLength?: number
  }>
}

// 设为默认模型
aiModels.setDefault({ id }) → { success: boolean }

// 同步到 OpenClaw
aiModels.syncToGateway({ id }) → { success: boolean }
```

---

## 四、前端界面设计

### 4.1 通讯渠道配置页面

```
┌─────────────────────────────────────────────────────────────────┐
│  🔌 CHANNELS                                    [+ Add Channel] │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 📱 Telegram Bot                              ✅ ACTIVE   │   │
│  │ @your_bot_username                                          │   │
│  │ Last tested: 2 mins ago                                  │   │
│  │ [Test] [Edit] [Sync] [Delete]                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 💬 Discord Bot                               ⚪ INACTIVE │   │
│  │ Not configured                                           │   │
│  │ [Configure]                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 💼 Slack App                                 ⚪ INACTIVE │   │
│  │ Not configured                                           │   │
│  │ [Configure]                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 AI 模型配置页面

```
┌─────────────────────────────────────────────────────────────────┐
│  🤖 AI PROVIDERS                               [+ Add Provider] │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🌐 OpenRouter                    ⭐ DEFAULT   ✅ ACTIVE  │   │
│  │ API Key: sk-or-v1-****...b59f                            │   │
│  │ Models: 200+ available                                   │   │
│  │ Last tested: Just now ✓                                  │   │
│  │ [Test] [Edit] [Set Default] [Delete]                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🟢 OpenAI                                    ⚪ INACTIVE │   │
│  │ Not configured                                           │   │
│  │ [Configure]                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 🟣 Anthropic (Claude)                        ⚪ INACTIVE │   │
│  │ Not configured                                           │   │
│  │ [Configure]                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 配置表单 Dialog

```
┌─────────────────────────────────────────────────────────────────┐
│  Configure OpenRouter                                      [X]  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Name                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ My OpenRouter                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  API Key *                                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ sk-or-v1-••••••••••••••••••••••••••••••••••••••••••••   │ 👁 │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Base URL (Optional)                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ https://openrouter.ai/api/v1                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ☑ Enable this provider                                        │
│  ☐ Set as default                                              │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🔍 Test Connection                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Test Result:                                                   │
│  ✅ Connection successful! Found 247 available models.         │
│  Latency: 234ms                                                 │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                    [Cancel]  [Save & Sync]      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、测试连接实现

### 5.1 通讯渠道测试

| 渠道 | 测试方法 |
|------|----------|
| Telegram | 调用 getMe API 验证 Bot Token |
| Discord | 调用 /users/@me API 验证 Token |
| Slack | 调用 auth.test API 验证 Token |
| WhatsApp | 调用 Graph API 验证 Access Token |
| 飞书/Lark | 调用 tenant_access_token API |
| iMessage | 连接 BlueBubbles/AirMessage 服务器 |

### 5.2 AI 模型测试

| 提供商 | 测试方法 |
|--------|----------|
| OpenAI | 调用 /models API 获取模型列表 |
| Anthropic | 发送简单消息测试 API Key |
| OpenRouter | 调用 /models API |
| Gemini | 调用 models.list API |
| MiniMax | 发送测试请求 |
| 其他 | 调用 /models 或发送测试消息 |

---

## 六、安全考虑

1. **敏感数据加密**
   - API Key、Token 等敏感字段使用 AES-256 加密存储
   - 前端显示时只显示部分字符（如 `sk-or-v1-****...b59f`）

2. **访问控制**
   - 只有管理员可以访问配置页面
   - 所有 API 都需要认证

3. **审计日志**
   - 记录所有配置变更
   - 记录测试连接操作

4. **速率限制**
   - 测试连接 API 限制每分钟 10 次
   - 防止滥用

---

## 七、实现步骤

### Phase 1: 数据库和 API（预计 1 小时）
- [ ] 创建数据库表
- [ ] 实现 tRPC API
- [ ] 添加加密/解密工具

### Phase 2: 通讯渠道配置（预计 1.5 小时）
- [ ] 通讯渠道列表页面
- [ ] 添加/编辑渠道 Dialog
- [ ] 各渠道测试连接实现
- [ ] 同步到 OpenClaw Gateway

### Phase 3: AI 模型配置（预计 1.5 小时）
- [ ] AI 提供商列表页面
- [ ] 添加/编辑提供商 Dialog
- [ ] 各提供商测试连接实现
- [ ] 获取可用模型列表
- [ ] 同步到 OpenClaw Gateway

### Phase 4: 测试和优化（预计 0.5 小时）
- [ ] 单元测试
- [ ] 集成测试
- [ ] UI 优化

---

## 八、确认问题

在开始实现之前，请确认以下问题：

1. **加密方式**：是否需要使用特定的加密算法？默认使用 AES-256-GCM。

2. **同步策略**：配置保存后是否自动同步到 OpenClaw Gateway？还是需要手动点击同步？

3. **多配置支持**：同一渠道类型是否支持多个配置？（如多个 Telegram Bot）

4. **优先级**：哪些渠道和模型提供商是最优先需要支持的？

请确认方案后，我将开始实现。
